// Where the backend API lives. Edit this one line for your deployment:
//
//   - Local dev, backend running via `cd backend && npm start` (port 3000):
//     the default below already points at it.
//   - Frontend and backend served from the same host/port in production
//     (e.g. you put a reverse proxy in front of both): set this to "".
//   - Frontend hosted separately (GitHub Pages, Netlify, Vercel, S3, ...)
//     from the backend (Render, Railway, Fly.io, a VPS, ...): set it to your
//     backend's full URL, no trailing slash.
//
// Example: window.ASK_ERP_API_BASE = "https://ask-the-erp-api.onrender.com";
window.ASK_ERP_API_BASE = "http://localhost:3000";
