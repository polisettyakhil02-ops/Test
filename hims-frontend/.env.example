# Base URL of the hims-backend API. Leave unset in local dev to use the
# Vite dev-server proxy configured in vite.config.ts ("/api" -> backend).
# Set this for a production build where the API is on a different origin.
VITE_API_BASE_URL=
